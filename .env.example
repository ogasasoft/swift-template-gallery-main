# Environment Variables (Optional for Docker)

# PORT is set in docker-compose.yml, but you can override here
PORT=3000
NODE_ENV=production

# If you need to override environment-specific settings:
# VITE_API_URL=https://api.example.com
