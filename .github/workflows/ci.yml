name: CI/CD Pipeline

# This workflow is maintained in the shared workspace repository.
# For local testing, copy from: .github/workflows/ci-shared.yml
# For remote CI, the shared workflow will be used when projects reference it.
